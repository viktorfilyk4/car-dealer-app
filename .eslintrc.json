// "prettier" cancels conflicting rules with ESlint

{
  "extends": ["next/core-web-vitals", "next/typescript", "prettier"],
  "ignorePatterns": ["**/*.json"],
  "rules": {
    "no-unused-vars": "error"
  }
}
